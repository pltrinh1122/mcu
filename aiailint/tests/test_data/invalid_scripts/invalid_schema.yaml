metadata:
  name: "invalid-schema-test"
  version: "1.0.0"
  description: "Test script with invalid schema"
  # Missing required fields and invalid data types
steps:
  - invalid_field: "This field is not in the schema"
    command: "echo 'test'"
    # Missing required 'name' field
  - name: 123  # Invalid type - should be string
    command: "echo 'test'"
  - name: "test-step"
    # Missing required 'command' field
